# 项目全量审查清单

> 审查日期：2026-09-09
>
> 审查方式：只读检查源码、测试、Maven 配置、资源文件、README、CHANGELOG，并执行本地构建与测试。
>
> 修改记录：2026-09-09 14:01，标记第 1、2 项处理结果，并补充第 3 项的实际 XSS/JSON 行为说明；原因是对应代码修复、测试和 README 调整已完成或确认。
>
> 当前状态：第 1、2 项已完成处理，第 3 项已确认实际行为并同步 README，其他项目待逐项处理。

## 一、审查范围与验证结果

- 扫描 15 个 Maven 模块，覆盖源码、测试、POM、资源文件、README 和 CHANGELOG。
- 共检查约 256 个非构建文件，其中 Java 文件约 230 个；`target` 等构建产物不作为源码问题进行审查。
- `mvn -T 4 '-Dserver.port=0' test`：全部 15 个模块测试通过。
- `mvn -T 4 '-DskipTests' package`：构建通过。
- 新增本报告前，`git status --short` 工作区干净；新增后仅包含本报告；`git diff --check`：无空白错误。
- 默认执行 `mvn -T 4 test` 时，集成测试会因多个模块同时占用 8080 端口而失败；指定随机端口后通过。
- 未连接真实 Redis、数据库或外部第三方服务，因此相关结论基于源码、依赖树和本地可复现运行时验证。

## 二、确定缺陷

### 1. 高优先级：`java.util.Date` 日期格式与文档、测试不一致

依据：

- README 宣称默认 `Date` 格式为 `yyyy-MM-dd`：
  [README.md:749](D:/Project/github/spring-boot-starter/README.md:749)
- MVC 配置实际使用完整的 `dateTimePattern` 创建 `DateFormatter`：
  [jakarta VeloWebMvcConfigurer.java:57](D:/Project/github/spring-boot-starter/velo-autoconfigure-jakarta/src/main/java/io/github/luminion/velo/web/VeloWebMvcConfigurer.java:57)
- `javax` 模块存在相同实现。
- 直接验证 `yyyy-MM-dd HH:mm:ss` 的 `DateFormatter` 无法解析 `2024-01-02`；现有集成测试只覆盖完整日期时间格式。

影响：Controller 请求参数、表单参数或查询参数使用日期-only 字符串时可能解析失败；同时用户无法根据 README 判断实际契约。

状态：已处理。默认 `Date` 输入支持 `yyyy-MM-dd HH:mm:ss` 和 `yyyy-MM-dd`，Spring `@DateTimeFormat` 与 Jackson `@JsonFormat` 的显式格式优先，默认时区继续使用全局 `GMT+8`。

### 2. 高优先级：关闭 Jackson 日期时间扩展后，非法格式仍会触发异常

Jackson 2 和 Jackson 3 自动配置会在判断 `velo.jackson.date-time-enabled=false` 之前创建 `SimpleDateFormat` 和 `DateTimeFormatter`：

- [Jackson 2 VeloJacksonAutoConfiguration.java:68](D:/Project/github/spring-boot-starter/velo-autoconfigure-jackson2/src/main/java/io/github/luminion/velo/jackson/VeloJacksonAutoConfiguration.java:68)
- [Jackson 3 VeloJacksonAutoConfiguration.java:60](D:/Project/github/spring-boot-starter/velo-autoconfigure-jackson3/src/main/java/io/github/luminion/velo/jackson/VeloJacksonAutoConfiguration.java:60)

直接验证：设置 `velo.jackson.date-time-enabled=false` 并配置非法格式 `invalid[`，仍会抛出 `IllegalArgumentException`。

影响：用户明确关闭该功能后，仍可能因为无效日期配置导致启动或创建 `ObjectMapper` 失败。

状态：已处理。日期时间格式对象仅在 `velo.jackson.date-time-enabled=true` 时创建；关闭该能力后，非法日期格式不会阻止 Jackson 构建。

### 3. 高优先级：启用 XSS 后，Jackson JSON 请求体中的字符串也会被清洗

README 表述 XSS 不接管 Jackson JSON 请求体：

- [README.md:594](D:/Project/github/spring-boot-starter/README.md:594)

但 Jackson 2/3 自动配置会获取 `XssCleaner`，并注册全局 `String` 反序列化器：

- [Jackson 2 VeloJacksonAutoConfiguration.java:126](D:/Project/github/spring-boot-starter/velo-autoconfigure-jackson2/src/main/java/io/github/luminion/velo/jackson/VeloJacksonAutoConfiguration.java:126)
- [Jackson 3 VeloJacksonAutoConfiguration.java:112](D:/Project/github/spring-boot-starter/velo-autoconfigure-jackson3/src/main/java/io/github/luminion/velo/jackson/VeloJacksonAutoConfiguration.java:112)

现有测试已证明，JSON 中的 `<b>safe</b>` 会被反序列化为 `safe`，而非保留原始字符串。

影响：富文本、HTML 片段或其他业务字符串可能被意外修改。若“所有 JSON 字符串都清洗”是设计目标，则 README 不正确；若 XSS 只应处理 MVC 参数，则代码不正确。

状态：已确认实际行为并同步 README。当前代码保持：启用 XSS 和 Jackson 字符串转换时，JSON 请求体中的普通字符串也会清洗，`@XssIgnore` 可显式放行；代码边界暂不调整。

### 4. 高优先级：Boot 4 缓存回退逻辑硬编码使用 Jackson 2

公共缓存配置的默认 Redis 序列化器为 `GenericJackson2JsonRedisSerializer`：

- [VeloCacheConfiguration.java:67](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/cache/VeloCacheConfiguration.java:67)

Boot 4 使用 Jackson 3。经过运行时验证，在存在 Redis 相关类、关闭 Jackson 3 自动配置且未提供自定义序列化器时，创建缓存配置会抛出：

`NoClassDefFoundError: com/fasterxml/jackson/databind/...`

影响：Boot 4 项目在关闭 Jackson 3 或缺少自定义 Redis 序列化器时，缓存自动配置可能启动失败。

状态：已确认。

## 三、测试与工程问题

### 5. 中优先级：Boot 2/3/4 集成测试固定占用 8080 端口

集成测试通过 `SpringApplication.run` 启动 Web 服务，但没有设置随机端口。多模块并行测试时会互相抢占 8080：

- [Boot4MockMvcIntegrationTest.java:40](D:/Project/github/spring-boot-starter/velo-spring-boot4-autoconfigure/src/test/java/io/github/luminion/velo/autoconfigure/Boot4MockMvcIntegrationTest.java:40)
- Boot 2、Boot 3 集成测试存在相同模式。

验证结果：默认 `mvn -T 4 test` 会失败；增加 `-Dserver.port=0` 后全量测试通过。

影响：测试结果依赖执行顺序和机器端口状态，容易产生误报。

状态：已确认，属于测试基础设施缺陷，不是业务运行时必现故障。

## 四、安全与配置风险

### 6. 中优先级：CORS 默认配置为允许任意来源并允许凭证

CORS 功能默认关闭，但开启后默认值为 `allowed-origin-patterns=*` 与 `allowCredentials=true`：

- [VeloProperties.java:515](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/VeloProperties.java:515)
- [README.md:844](D:/Project/github/spring-boot-starter/README.md:844)

代码只记录警告，不阻止该组合生效。用户若只开启 `velo.web.cors.enabled=true`，可能无意中放大跨域请求和 CSRF 风险。

状态：设计风险，需确认是否调整默认值或改为强制显式配置来源。

### 7. 中优先级：Jackson 2 Redis 序列化使用宽松的 Default Typing

Jackson 2 配置使用 `LaissezFaireSubTypeValidator` 和 `DefaultTyping.NON_FINAL`：

- [VeloJacksonAutoConfiguration.java:157](D:/Project/github/spring-boot-starter/velo-autoconfigure-jackson2/src/main/java/io/github/luminion/velo/jackson/VeloJacksonAutoConfiguration.java:157)

如果 Redis 数据可能被非完全可信方写入，存在多态反序列化安全风险；如果 Redis 始终是受信任基础设施，则主要是安全审查项。

状态：潜在安全风险，需确认 Redis 信任边界。

### 8. 中优先级：结构化日志字段未转义

异常摘要会被直接包在引号中写入日志，未处理引号、换行符和反斜杠：

- [Slf4JInvocationLogWriter.java:101](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/log/support/Slf4JInvocationLogWriter.java:101)
- [Slf4JInvocationLogWriter.java:146](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/log/support/Slf4JInvocationLogWriter.java:146)

影响：用户输入进入异常消息时，可能破坏结构化日志格式或造成日志注入。

状态：潜在安全与可观测性问题。

## 五、容易产生歧义或低优先级问题

### 9. “只告警”与实际配置行为不一致

`VeloPropertiesWarningLogger` 对多项配置只记录警告，但日期格式等配置会在后续自动配置中直接抛异常；横幅输出也直接访问多个嵌套配置对象，显式配置为空时可能出现空指针。

涉及：

- [VeloPropertiesWarningLogger.java:42](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/core/VeloPropertiesWarningLogger.java:42)
- [VeloBannerPrinter.java:42](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/core/VeloBannerPrinter.java:42)

### 10. 全局枚举映射为空时，显式 `@JsonEnum` 映射也会被跳过

`JsonEnumMetadataResolver` 在全局 `enumMappings` 为空时直接返回，不再解析显式注解：

- [JsonEnumMetadataResolver.java:23](D:/Project/github/spring-boot-starter/velo-autoconfigure-common/src/main/java/io/github/luminion/velo/jackson/support/JsonEnumMetadataResolver.java:23)

如果全局配置为空的语义是“关闭所有枚举增强”，这是文档表达问题；如果显式注解应独立生效，则属于功能缺陷。

### 11. README 中 Excel 转换器启用说明互相矛盾

一处说明需要显式开启 `velo.excel.converters.enabled`，另一处又说明该配置默认值为 `true`，代码行为也以默认开启为准：

- [README.md:214](D:/Project/github/spring-boot-starter/README.md:214)
- [README.md:277](D:/Project/github/spring-boot-starter/README.md:277)

### 12. 异常处理基类存在宽范围组件扫描风险

`VeloWebExceptionHandler` 和 `VeloValidationWebExceptionHandler` 是带必需构造参数的 `@RestControllerAdvice` 类。用户若扫描 `io.github.luminion` 等宽范围包，可能将基类直接注册为组件，并因缺少 `Function` Bean 启动失败。

### 13. 模式配置未执行空白裁剪

`VeloModeEnvironmentPostProcessor` 对原始值直接执行大写转换，不执行 `trim()`；例如 `" conservative "` 不会被识别为合法模式。

### 14. Excel 辅助类返回不可修改的转换器列表

`EasyExcelHelper`、`FastExcelHelper`、`FesodExcelHelper` 的扩展转换器列表使用 `Collections.unmodifiableList` 返回。是否允许调用方继续追加转换器，当前 API 契约没有明确说明。

## 六、逐项处理顺序

建议按照以下顺序逐项确认和处理：

1. `java.util.Date` 日期契约
2. Jackson 日期时间禁用逻辑
3. XSS 与 JSON 请求体边界
4. Boot 4 Redis 缓存序列化器
5. 集成测试随机端口
6. CORS 默认安全策略
7. Redis Default Typing 安全策略
8. 日志字段转义
9. 其余配置、枚举、文档和 API 歧义

每确认一项后，再修改对应文件、补充测试并重新验证，然后进入下一项。
